{
  "name": "workbench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "make -C v3 setup",
    "dev": "make -C v3 serve",
    "dev:frontend": "npm --prefix v3/frontend run dev",
    "build": "npm --prefix v3/frontend run build",
    "test": "npm --prefix v3/frontend test && cd v3 && uv run pytest",
    "check": "npm --prefix v3/frontend run check && cd v3 && uv run ruff check backend && uv run ruff format --check backend",
    "export": "make -C v3 export"
  }
}
