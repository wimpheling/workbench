{
  "name": "workbench-engineering",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "make -C v3 serve",
    "build": "npm --prefix v3/frontend run build",
    "test": "npm run test:engineering && npm --prefix v3/frontend test",
    "test:engineering": "vitest run --config engineering/vitest.config.ts",
    "check": "make -C v3 check"
  },
  "dependencies": {
    "replicad": "^0.23.1",
    "replicad-opencascadejs": "^0.23.0"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
